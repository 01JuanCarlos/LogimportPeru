---
import "../styles/global.css";
import Layout from "../layouts/Layout.astro";

const pageTitle = "Cotizacion enviada";
const pageDescription =
  "Servicios de importación, aduanas y cadena de suministro en Perú. Optimiza tus costos y tiempos con Logimport.";
const pageCanonical = "https://tudominio.com/";
---

<style>
  :root {
    --card: #0b1220;
    --accent: #10b981; /* verde */
    --muted: #a6b0c3;
    --glass: rgba(255, 255, 255, 0.03);
    --radius: 16px;
    font-family:
      Inter,
      system-ui,
      -apple-system,
      "Segoe UI",
      Roboto,
      "Helvetica Neue",
      Arial;
  }
  * {
    box-sizing: border-box;
  }
  html,
  body {
    height: 100%;
  }
  body {
    margin: 0;
    color: #e6eef8;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 24px;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }
  .card {
    width: 100%;
    max-width: 720px;
    background: linear-gradient(
      180deg,
      rgba(255, 255, 255, 0.02),
      rgba(255, 255, 255, 0.01)
    );
    border-radius: var(--radius);
    padding: 36px;
    box-shadow: 0 10px 30px rgba(2, 6, 23, 0.6);
    border: 1px solid rgba(255, 255, 255, 0.03);
    display: flex;
    gap: 24px;
    align-items: center;
  }
  .icon {
    flex: 0 0 96px;
    height: 96px;
    border-radius: 50%;
    background: linear-gradient(
      135deg,
      rgba(16, 185, 129, 0.14),
      rgba(16, 185, 129, 0.08)
    );
    display: grid;
    place-items: center;
    border: 1px solid rgba(16, 185, 129, 0.12);
    animation: pop 0.6s ease;
  }
  @keyframes pop {
    from {
      transform: scale(0.7);
      opacity: 0;
    }
    to {
      transform: scale(1);
      opacity: 1;
    }
  }
  .content {
    flex: 1 1 auto;
  }
  h1 {
    margin: 0 0 6px 0;
    font-size: 20px;
    letter-spacing: 0.2px;
  }
  p {
    margin: 0 0 12px 0;
    color: var(--muted);
    line-height: 1.45;
  }
  .meta {
    font-size: 13px;
    color: var(--muted);
    margin-top: 6px;
  }
  .actions {
    display: flex;
    gap: 12px;
    margin-top: 18px;
  }
  .btn {
    appearance: none;
    border: 0;
    padding: 10px 16px;
    border-radius: 10px;
    font-weight: 600;
    cursor: pointer;
  }
  .btn-primary {
    background: linear-gradient(90deg, var(--accent), #06a66e);
    color: white;
    box-shadow: 0 6px 18px rgba(16, 185, 129, 0.14);
  }
  .btn-link {
    background: transparent;
    color: var(--muted);
    border: 1px solid rgba(255, 255, 255, 0.03);
  }
  footer {
    margin-top: 14px;
    font-size: 12px;
    color: var(--muted);
  }
  @media (max-width: 640px) {
    .card {
      flex-direction: column;
      align-items: center;
      text-align: center;
      padding: 28px;
    }
    .icon {
      flex: 0 0 84px;
      height: 84px;
    }
    .actions {
      justify-content: center;
      width: 100%;
    }
  }
</style>

<Layout
  title={pageTitle}
  description={pageDescription}
  canonical={pageCanonical}
>
  <main class="card" role="main" aria-labelledby="title">
    <svg
      width="48"
      height="48"
      viewBox="0 0 24 24"
      fill="none"
      xmlns="http://www.w3.org/2000/svg"
      aria-hidden="true"
    >
      <circle
        cx="12"
        cy="12"
        r="11"
        stroke="rgba(16,185,129,0.22)"
        stroke-width="2"></circle>
      <path
        d="M7 12.5l2.6 2.6L17 8.7"
        stroke="#10b981"
        stroke-width="1.8"
        stroke-linecap="round"
        stroke-linejoin="round"></path>
    </svg>

    <div
      class="content"
      style="    text-align: center;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;"
    >
      <h1 id="title">¡Cotización enviada con éxito!</h1>
      <p>
        Gracias por contactarnos. Hemos recibido tu solicitud de cotización y
        nuestro equipo te responderá a la brevedad.
      </p>

      <div class="actions" role="group" aria-label="Acciones">
        <a class="btn btn-primary" href="/">Volver al inicio</a>
      </div>
    </div>
  </main>
</Layout>
